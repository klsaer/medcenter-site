# В репозитории — всегда LF, в рабочей копии Windows — CRLF
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
# Отсоединённая подпись — побайтовый документ. Без этой строки git
# нормализует CRLF в текстовых .sig и подпись перестаёт проверяться.
*.sig binary
*.woff  binary
*.woff2 binary
