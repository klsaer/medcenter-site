# сборка — генерируется командой `node build.js`
dist/

# зависимостей у проекта нет, но на всякий случай
node_modules/

# системное
Thumbs.db
desktop.ini
.DS_Store

# редакторы
.vscode/
.idea/
*.swp

# Исходники от клиента. В репозиторий не идут: репозиторий публичный,
# а в них есть данные, которые публиковать нельзя (СНИЛС врача в excerpt.pdf).
# То, что можно и нужно публиковать, лежит в assets/docs.
Лицензия с qr/
Информация/
